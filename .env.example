# Groq API key (get one at https://console.groq.com/keys).
# Server-side only — never exposed to the browser.
GROQ_API_KEY=

# Optional: override the Groq model used for fact extraction and enhancement.
# GROQ_MODEL=openai/gpt-oss-120b
